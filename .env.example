# Copy this file into `.env` and fill in the values if you want to run Kamal commands from your local machine.
# Make sure to prefix your Kamal commands with `dotenv` to autoload the `.env` file into the environment.

SERVER_IP=         # Server IP address.
GITHUB_TOKEN=      # GitHub token generated by running `gh config get -h github.com oauth_token`.
RAILS_MASTER_KEY=  # Rails master key stored in `config/master.key`.
POSTGRES_PASSWORD= # PostgreSQL DB password.
MEILI_MASTER_KEY=  # Meilisearch API key.
SHIPYRD_HOST=      # Shipyrd host: shipyrd.ieasybooks.com.
SHIPYRD_API_KEY=   # The application Shipyrd API key.
